# 年1回のデータ更新手順（Standard月）

このシステムは普段 J-Quants の **Light プラン（月1,650円）** で自動運用されているが、
以下のデータは **Standard プラン（月3,300円）** でしか取得できないため、
**年に1回だけ Standard に切り替えて更新する**（更新後すぐ Light に戻す＝追加費用は年1,650円差額のみ）。

- **10年分の日足** … バックテストの勝率較正・戦略検証の土台（Lightは5年まで）
- **信用残**（銘柄別・週次） … 需給研究用
- **空売り比率**（33業種・日次） … 需給研究用

## 手順（合計15分・作業はブラウザだけ）

### 1. J-Quants を Standard にする
1. https://jpx-jquants.com/ にログイン
2. プラン変更で **Standard** を選択
3. マイページで **APIキー** を確認してコピー

### 2. GitHub のキーを更新する（キーが変わった場合のみ）
1. このリポジトリの **Settings → Secrets and variables → Actions** を開く
2. `JQUANTS_API_KEY` の **Update** を押し、新しいキーを貼り付けて保存

### 3. 更新ボタンを押す
1. リポジトリの **Actions** タブを開く
2. 左の一覧から **「年1回データ更新（Standard月に実行）」** を選ぶ
3. 右上の **「Run workflow」→ 緑の「Run workflow」** を押す
4. 5〜10分待って、全ステップが ✅ 緑になれば完了
   - ❌ 赤で止まった場合はログに日本語で原因が出る（ほぼ「プラン未変更」か「キー未更新」のどちらか）

### 4. J-Quants を Light に戻す ← 忘れると翌月も3,300円！
1. J-Quants のマイページでプランを **Light** に戻す
2. （キーが再発行された場合は手順2と同じ要領で `JQUANTS_API_KEY` を戻す）

### 5. ローカルの観測スペース（http://127.0.0.1:8790/）にも反映する
- `auto-kabu-screener` フォルダの **`最新データ取り込み.bat` をダブルクリック**するだけ
- （GitHubのボタンで更新した内容をローカルにコピーする操作。普段のデータも古いと感じたら
  いつでもこのバットで最新化できる）

## プランの使い分け（重要）

| プラン | 契約 | 役割 |
|---|---|---|
| **Light（月1,650円）** | **常時契約（解約しない）** | 毎日の運用の生命線。日足・銘柄マスタ・投資部門別
（これが無いと買い目標の再計算・ランキング・全市場スクリーニング・LINE通知が全部止まる） |
| **Standard（月3,300円）** | **年1回・1ヶ月だけ** | このページの年次更新（10年較正・信用残・空売り比率）専用 |

## 更新されるもの

| ファイル | 内容 |
|---|---|
| `docs/fund-flow-ai-system/data/backtest.json` | 10年分の日足による勝率較正・標準/ゆるめ比較（観測スペースの winRate 較正表示の元） |
| `research-data/margin-interest.json` | 監視銘柄の信用残 10年分 |
| `research-data/short-ratio.json` | 33業種の空売り比率 10年分 |

## 2027年の更新へのメモ: Standard ではなく Premium（月16,500円）を検討

Premium は Standard の全データを含むため、**年1回の更新月だけ Premium にすれば差額13,200円**で
以下が追加で手に入る（2026-07-10 の検討結論）:

1. **20年分の日足** → バックテストを20年に延長し、リーマンショック(2008)込みの
   ストレステストができる（現在の10年較正の最悪年は2018年どまり）。**これが本命**
2. 配当金の詳細（正確な落ち日・金額）→ 権利落ち日対策を「見送り」から「目標補正」へ精緻化できる
3. 売買内訳（銘柄別の空売り含む約定内訳）→ 需給シグナルとして検証可能。
   ただし業種別空売り・銘柄別信用残はどちらも予測力なしだったので**期待は控えめに**

手順は本ページと同じ（プラン名が Premium になるだけ）。20年バックテストはローカルで
`BACKTEST_HISTORY_DAYS=7300 node scripts/backtest-strategy.js --refresh` を実行し、
結果が良くても悪くても較正は10年のまま使う（20年前の市場構造は現在と違いすぎるため参考値）。
Premium常時契約は年198,000円で運用益を食うため論外。

## 補足

- 普段の自動運用（20分毎の市場データ更新・観測スペース・LINE通知・全市場スクリーニング）は
  Light のままで全部動く。この年次更新をサボっても止まるものはなく、較正が古くなるだけ。
- ローカルPCでやりたい場合は `auto-kabu-screener` フォルダで以下でも同じ（`.env` の
  `JQUANTS_REFRESH_TOKEN` を新キーにしてから）:
  ```
  BACKTEST_HISTORY_DAYS=3650 node scripts/backtest-strategy.js --refresh
  node scripts/fetch-margin-short.js
  ```
