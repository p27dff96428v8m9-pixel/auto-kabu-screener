# ローカル単独運転の手引き（GitHubなしで動かす）

GitHubが**障害・有料化・サービス終了**になっても、このPCだけでシステム全体
（20分毎のデータ更新→買い目標到達判定→LINE通知、夜の全市場スキャン、
21時の前夜ダイジェスト、月初の月次答え合わせ）を動かし続けるための非常運転モード。

## 使い方

1. **`ローカル単独運転.bat` をダブルクリック**（フォルダ直下にある）
2. 警告を読んで何かキーを押すと運転開始。以後20分毎に自動で回り続ける
3. 観測スペースは http://127.0.0.1:8790/ （サーバーも自動起動される）
4. **止めるときはウィンドウを閉じるだけ**
5. GitHubに復帰するときは `最新データ取り込み.bat` を実行
   （ローカル運転中のデータはGitHub側で上書きされて消える。成績を残したい場合は
   ウィンドウを閉じる前に `docs/fund-flow-ai-system/data/integrated-obs.json` をコピーしておく）

## ⚠️ 一番大事な注意

**GitHub Actionsが動いている通常時に起動しない**こと。LINE通知が二重に届きます。
これは「GitHubが止まったとき・やめたとき」専用のモードです。

## 事前設定（.env に書く。最初に1回だけ）

`auto-kabu-screener` フォルダの `.env` をメモ帳で開くと、末尾にテンプレートがある。
`#` を外して値を入れると有効になる：

| 設定 | 効果 | 未設定だと |
|---|---|---|
| `LINE_ACCESS_TOKEN` / `LINE_USER_ID` | ローカル運転でもLINE通知が届く（GitHubのSecretsと同じ値） | 通知なしで判定だけ動く |
| `OBS_PRICE_SOURCE=tachibana` + `TACHIBANA_USER_ID` / `TACHIBANA_PASSWORD` | 立花証券のリアルタイム価格で判定 | Yahoo遅延価格（約20分遅れ）で判定 ※GitHub運転と同じ既定 |

※ 立花証券の実装は「リアルタイム価格の取得」。自動発注は未実装（実弾はLINE通知を見て手動発注）。

## ローカル運転の制約（正直な注意点）

- **PCが点いている間しか動かない**。平日9:00〜15:30にスリープすると到達検知が止まる
  （Windowsの電源設定でスリープを「なし」にしておくこと）
- スマホから観測スペースは見えない（127.0.0.1は自宅PC内のみ）。LINE通知の本文は届くので
  売買判断は可能
- 年1回のStandard月更新もローカルで可能：
  `BACKTEST_HISTORY_DAYS=3650 node scripts/backtest-strategy.js --refresh` と
  `node scripts/fetch-margin-short.js`（詳細は ANNUAL_UPDATE.md の補足）
