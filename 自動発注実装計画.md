# 自動発注 実装計画書（未来のAI/開発者への引き継ぎ書）

このファイルは「資金が育ったら立花証券APIで自動発注を実装する」ための設計書。
ユーザーが「**この計画書の通りに自動発注を作って。資金は◯円**」と言えば、
これを読んだAIがそのまま実装に入れるように、決定済みの設計判断と技術情報を全部書いてある。

作成: 2026-07-10（戦略監査3回・10年バックテスト・6バケット検証を経た時点の結論に基づく）

---

## 1. 実装を開始する条件（全部揃ってから）

- [ ] 運用資金が **300万円以上**（立花証券は単元株のみでS株不可。300万未満だと
      「資金の10%/銘柄」ルールが単元制約で組めず分散が壊れるため）
- [ ] 観測スペースの**月次答え合わせ**（毎月1〜3日のLINE）で、実測とバックテスト理論値が
      数ヶ月連続で大きく乖離していないこと（乖離していたら自動化ではなく原因調査が先）
- [ ] 立花証券e支店の口座開設・API利用申請・**第二パスワード**（注文用）の発行が完了
- [ ] GitHub Secrets（またはローカル.env）に TACHIBANA_USER_ID / TACHIBANA_PASSWORD /
      TACHIBANA_SECOND_PASSWORD を設定済み

## 2. 何を自動化するか（決定済みの方針）

### エントリー
- **candidateRanking の第1候補バケットにだけ従う**（integrated-obs.json の
  `candidateRanking.items[0]`。2026-07時点では ゆるめ×100万固定。実測で入れ替われば自動追従）
- update-integrated-obs.js が買い目標到達（buyEvents）を検知したタイミングで発注
- 注文種別: **指値**（到達価格近辺）。寄らなければ当日限りで失効させる

### エグジット（最重要の設計判断）
- **建玉と同時に OCO 注文（利確指値 tp ＋ 損切逆指値 sl）を証券会社側に置く**
- 理由: サーバー判定は最短でも20分遅れ＋GitHub障害リスクがある。損切りを
  システムの死活に依存させない（2026-07-08決定の「実弾はOCO/逆指値方針」）
- tp/sl の値は観測スペースの目標値（ATRベース）をそのまま使う
- 保有126日超の時間切れ手仕舞いは、サーバーの timeout 判定通知を受けて成行返済

### サイズ
- 1銘柄 = **そのときの資金の約10%** を単元数に丸め（買えない高額銘柄はスキップして記録）
- **底値圏ブースト日**（regime.bottomZone=true）だけ 2〜3倍（環境変数で倍率設定）
- 信用2倍（E型）は**この計画には含めない**。現物で数ヶ月運用して実測を見てから別途判断

## 3. 安全装置（必須要件・省略禁止）

1. **DRY_RUNモード**: `AUTO_TRADE=dry` で「発注する内容をLINE通知するだけ・実発注なし」。
   **最低2週間のdry運転で内容が正しいことを確認してから `on` にする**
2. **killスイッチ**: `AUTO_TRADE=off`（GitHub Variables / ローカル.env）で即時完全停止
3. **上限**: 1日の新規発注 3件まで（ブースト日は6件）・1銘柄あたり資金の15%を超えない・
   投入合計が資金を超えない（現金残高を注文照会APIで実額確認してから発注）
4. **二重発注防止**: 発注済み銘柄コードを状態ファイルに記録し、毎回 注文照会APIと
   突き合わせてから新規発注（サーバー再実行・GitHub/ローカル二重起動でも重複しないこと）
5. **全発注をLINE通知**（成功・失敗・失効すべて）
6. 想定外のAPI応答時は**発注せずに停止してLINEでエラー通知**（黙って続行しない）

## 4. 技術情報（実装の足がかり）

- **既存の立花API実装**: `scripts/update-integrated-obs.js` の `fetchTachibanaPrices()` に
  ログイン（CLMAuthLoginRequest→仮想URL取得）・リクエスト形式（URL?URLエンコードJSON）・
  Shift_JIS応答対応・ログアウトまで動作実績あり。**このコードを流用すること**
- 発注系は ログイン応答の `sUrlRequest`（取引系仮想URL）に対して:
  - 新規注文: `CLMKabuNewOrder`（現物買・sSecondPassword 必須）
  - 訂正/取消: `CLMKabuCorrectOrder` / `CLMKabuCancelOrder`
  - 注文照会: `CLMOrderList` 系、残高照会: `CLMZanKaiSummary` 系
  - 逆指値・OCOは注文パラメータの sCondition 系（**必ず https://www.e-shiten.jp/api/ の
    最新版仕様書でパラメータ名を確認**。バージョンでURLと項目名が変わる。
    TACHIBANA_API_BASE_URL 変数で新URLに切替可能な作りは価格取得側で実装済み）
- **置き場所**: `scripts/auto-order.js` を新設し、update-integrated-obs.js の buyEvents /
  settleEvents 処理の直後から呼ぶ（判定と発注のデータソースを完全に一致させるため）。
  GitHub Actions（update-market-data.yml）と **ローカル単独運転（scripts/local-runner.js）の
  両方から同じように動く**こと
- 環境変数設計: `AUTO_TRADE=off|dry|on` / `AUTO_TRADE_BUDGET_PCT=10` /
  `AUTO_TRADE_BOOST_MULT=2` / `AUTO_TRADE_DAILY_LIMIT=3` / `TACHIBANA_SECOND_PASSWORD`

## 5. 実装の進め方（推奨手順）

1. 注文照会・残高照会だけ実装して実口座で読み取り確認（発注コードはまだ書かない）
2. auto-order.js を DRY_RUN で実装 → 2週間、dry通知と実際の相場を突き合わせ
3. 最小サイズ（1単元・1銘柄のみ）で `on` → 1〜2週間の実弾検証
4. 上限を計画値まで開放

## 6. 背景資料（このリポジトリ内）

- 戦略の実証: `docs/fund-flow-ai-system/data/backtest.json`（10年較正）
- 第1候補ロジックの検証: `scripts/analyze-candidate-ranking.js`
- 運用ルールの根拠: ANNUAL_UPDATE.md / ローカル単独運転の手引き.md
- 実弾方針: 通知1件=資金の10%・決済は通知通り・ブースト日のみ2〜3倍・
  レバレッジと集中は封印（中央値を上げない・2026-07-10のモンテカルロで実証済み）
